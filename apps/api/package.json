{
  "name": "@leasora/api",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn leasora_api.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "uv build",
    "test": "uv run pytest",
    "lint": "uv run ruff check .",
    "type-check": "uv run mypy src/leasora_api",
    "clean": "rm -rf dist build *.egg-info .pytest_cache .mypy_cache"
  }
}
