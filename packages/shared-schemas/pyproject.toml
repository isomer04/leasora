[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[project]
name = "leasora-schemas"
version = "0.2.0"
description = "Leasora shared Pydantic schemas"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.9.0"]

[project.optional-dependencies]
dev = ["pytest", "mypy"]
